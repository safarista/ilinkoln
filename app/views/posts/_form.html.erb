<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="field">
    <%= f.label :title, "Article Title" %><br />
    <%= f.text_field :title, :required => true %>
	</div>
	<div class="field">
    <%= f.label :body, "Article " %><br />
    <%= f.text_area :body, :required => true %>
	</div>
	<div class="field">
		<ul>
			<li><h2>Category</h2></li>
	    <% Category.all.each do |category| %>
			<li>
	    		<label><%= f.radio_button :category_id, category.id%><label> <%= category.name.titleize %></label>
	  		</li>
		<% end -%>
		</ul>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
